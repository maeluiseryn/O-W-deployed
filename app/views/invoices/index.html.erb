<h1>Listing invoices</h1>
<p> Somme restante a payer: <%=project_remaining_to_pay(@invoices.first.project)  %>
<table>
  <tr>
    <th>Projet</th>
    <th>Somme Total</th>
    <th>Echeance</th>
    <th>Somme restante</th>
    <th>Date de création</th>
    <th></th>
    <th></th>
  </tr>


  <%= render :partial => 'invoice_list' ,:collection=>@invoices ,:as=>:invoice %>

</table>

<br />

<%= link_to 'Nouvelle Facture', new_project_invoice_path(@invoices.first.project_id) unless  project_remaining_to_pay_equal_zero(@invoices.first.project) %> 
<%= link_to 'Retour au projet', project_path(@invoices.first.project_id) %>