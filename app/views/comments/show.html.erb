

<p>
  <b>Description:</b>
  <%= @comment.description %>
</p>

<p>
  <b>Content:</b>
  <%= @comment.content %>
</p>

<p>
  <b>User:</b>
  <%= @comment.user_id %>
</p>
<p>
  <b>Creer le</b>
  <%= I18n.l(@comment.created_at) %>
</p>
<table>
  <%= render :partial =>'shared/contact_list' ,:collection=>@comment.contacts , :as =>:contact %>
</table>
<%= link_to 'Lu', read_comment_path(@comment) %>
<%= link_to 'Archive', archive_comment_path(@comment) %>
<%= link_to 'Corbeille', trash_comment_path(@comment) %>
<%= link_to 'Edit', edit_comment_path(@comment)unless correct_current_user(@comment)==false %> |
<%= redirect_back_to_owner_polymorphic(@comment) %>
